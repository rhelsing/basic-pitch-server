[phases.setup]
providers = ["python"]
aptPkgs = ["..."]

[phases.build]
cmds = ["echo start build", "sudo apt update", "sudo apt-get install libsndfile1", "...", "echo done build"]

[start]
cmd = "python3 app.py"
